import Image

import argparse

NUMBER_BINS=32 #Number of bins each
SIZE_PIXEL=0.1 #Real size of one pixel, in square centimeters

def resize_image(img_path,size,number_bins,size_pixel):
    img=PIL.Image.open(img_path)

if __name__ == '__main__':
